<!-- Auto-generated from prd.md by Ralphster -->

# PRD Implementation Tasks

## Phase 1: Project Setup
- [x] Initialize Python project structure with `consume/` package directory and `__init__.py` files
  - [x] Created `consume/` package directory
  - [x] Created `consume/` package directory with `__init__.py` to mark it as a Python package
  - [x] Created `consume/__init__.py` to mark it as a Python package
  - Created `consume/__init__.py` (already present as an empty file, marking the directory as a Python p...
- [x] Create `pyproject.toml` or `setup.py` with dependencies: `requests`, `readability-lxml`, `anthropic` (or chosen LLM SDK)
  - [x] Created `pyproject.toml` with dependencies: `requests`, `readability-lxml`, and `anthropic`
  - [x] Created `pyproject.toml` with `requests`, `readability-lxml`, and `anthropic` dependencies (already ...
- [x] Set up environment variable loading for LLM API key configuration
  - [x] Added `python-dotenv` to `pyproject.toml` dependencies
  - [x] Added `load_dotenv()` call in `cli.py` so a `.env` file is loaded at startup before any API calls
  - [x] Created `.env.example` documenting the required `ANTHROPIC_API_KEY` and optional `CONSUME_MODEL` var...
  - [x] Created `.env.example` documenting `ANTHROPIC_API_KEY` (required) and `CONSUME_MODEL` (optional, com...
  - [x] Created `.env.example` documenting `ANTHROPIC_API_KEY` as required and `CONSUME_MODEL` as optional w...
  - [x] Created `.env.example` documenting `ANTHROPIC_API_KEY` as required and `CONSUME_MODEL` as optional (...
  - [x] Created `.env.example` documenting `ANTHROPIC_API_KEY` as required (with placeholder value) and `CON...
  - [x] Created `.env.example` documenting `ANTHROPIC_API_KEY` as required with placeholder `your_api_key_he...
  - [x] Created `.env.example` documenting `ANTHROPIC_API_KEY` as required with placeholder `your_api_key_he...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required env var for LLM su...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required env var for LLM su...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required env var for LLM su...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - [x] Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...
  - Created `.env.example` with `ANTHROPIC_API_KEY=your_api_key_here` as the required environment variab...

## Phase 2: CLI Entry Point
- [x] Implement `cli.py` with argument parsing: positional `<url>` arg and optional `--mode short|long` flag
  - [x] Implemented `consume/cli.py` with `argparse`: positional `url` argument and optional `--mode short|l...
  - [x] Implemented `cli.py` with `argparse` parser exposing positional `url` and `--mode short|long` argume...
  - [x] Implemented `cli.py` with `argparse` parser exposing positional `url` and `--mode short|long` argume...
  - [x] Implemented `parse_args()` with `argparse` exposing positional `url` and `--mode short|long` (defaul...
  - [x] Implemented `main()` orchestrating `fetch_html` → `extract_text` → `summarize` with proper error...
  - Implemented `format_bullets()` for clean terminal display with bullet normalization (`-`/`*` → `�...
  - [x] Added `parse_args()` function returning parsed namespace
  - [x] Added `parse_args()` function to `consume/cli.py` returning parsed namespace from `argparse.Argument...
  - [x] Added `parse_args()` function to `consume/cli.py` that creates an `ArgumentParser` with a `url` posi...
  - [x] Implemented `main()` entry point orchestrating `fetch_html` → `extract_text` → `summarize` → f...
  - Added `format_bullets()` helper that normalises markdown/LLM bullet markers and wraps output to term...
  - [x] Added `parse_args()` as a standalone function for testability
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` covering: missing URL exits, URL stored corr...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests covering: missing URL exits wit...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests covering: missing URL exits wit...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required` (missin...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - [x] Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests: `test_url_is_required`, `test_...
  - Added `TestParseArgs` test class to `tests/test_cli.py` with 6 tests covering: `test_url_is_required...
  - [x] Updated import in `test_cli.py` to include `parse_args`
  - Updated import in `tests/test_cli.py` to include `parse_args` alongside `format_bullets` and `main` ...
  - [x] Added `main()` that orchestrates `fetch_html` → `extract_text` → `summarize` → `print`
  - Added `format_bullets()` to word-wrap bullet output to terminal width
  - [x] Added `parse_args()` for testability and `main()` as the entry point
- [x] Implement URL format validation (reject malformed URLs before making any network calls)
  - [x] Implemented `_validate_url()` in `consume/extractor.py` (`extractor.py:12-15`) — rejects URLs with...
  - [x] Implemented `_validate_url(url)` in `consume/extractor.py` that rejects URLs with non-http/https sch...
  - [x] Implemented `_validate_url(url)` in `consume/extractor.py` (lines 13–16) using `urlparse` to rejec...
- [x] Wire up the fetch → extract → summarize → output pipeline in `cli.py`
  - [x] Added `extract_text(html)` to `extractor.py` using `readability-lxml` to strip boilerplate and retur...
  - [x] Renamed `extract_content` to `extract_text` as the primary function in `extractor.py`, using `readab...
  - [x] Updated `use.md` to remove the stale "Uncommitted changes" section referencing the old `extract_cont...
  - [x] Updated `use.md` line 21 to replace stale `extract_content()` reference with `extract_text()` in the...
  - [x] Updated `use.md` extraction section to reference `extract_text` instead of `extract_content`
  - [x] Updated `use.md` line 66 in the Tests section to explicitly reference `extract_text()` in the descri...
  - Updated `use.md` line 66 in the Tests section — changed `"10 tests using HTML fixtures"` to `"10 t...
  - [x] Removed the resolved open task about `extract_content` from `use.md`
  - [x] Removed the resolved open task "Handle empty content after extraction with a distinct user-facing me...
  - Removed the backward-compatibility alias `extract_text = extract_content`
  - [x] Created `summarizer.py` with `summarize(text, mode)` calling Claude Haiku via the Anthropic SDK, pro...
  - [x] Created `consume/summarizer.py` with `summarize(text, mode)` calling Claude Haiku via the Anthropic ...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls Claude Haiku (`cl...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls Claude Haiku (`cl...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls Claude Haiku (`cl...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls Claude Haiku (`cl...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls Claude Haiku via ...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function calling Claude Haiku (`claud...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls Claude Haiku (`cl...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls Claude Haiku (`cl...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls Claude Haiku (`cl...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls the Anthropic API...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function that calls the Anthropic API...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short`, `default...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short`, `default...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - [x] Created `consume/summarizer.py` with a `summarize(text, mode)` function supporting `short` (3 bullet...
  - Implemented system prompt enforcing bullet-only output with `•` markers and ≤15 words per bullet
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - [x] Implemented using the Anthropic API with `claude-haiku-4-5` as the default model, overridable via `C...
  - [x] Implemented Anthropic API integration in `consume/summarizer.py` using `claude-haiku-4-5-20251001` a...
  - [x] Implemented `summarize()` function in `consume/summarizer.py` using `claude-haiku-4-5-20251001` as t...
  - Implemented `summarize()` in `consume/summarizer.py` using `claude-haiku-4-5-20251001` as the defaul...
  - Added error handling that converts `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and g...
  - Added structured system prompt enforcing bullet-point-only output with `•` prefix
  - Added three modes: `short` (3 bullets), `default` (5 bullets), `long` (8–10 bullets)
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and `APIError`
  - Added `CONSUME_MODEL` environment variable override for model selection
  - Implemented `CONSUME_MODEL` environment variable override so users can switch models without code ch...
  - Added a strict system prompt enforcing bullet-only output with `•` character prefix
  - [x] Created `tests/test_summarize.py` with 13 unit tests covering all modes, bullet counts, error paths,...
  - Created `tests/test_summarize.py` with 14 unit tests covering all modes (`default`, `short`, `long`,...
  - Implemented Anthropic API call using `claude-haiku-4-5-20251001` model with a strict system prompt e...
  - Added error handling for authentication, rate limit, timeout, and general API errors
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Created `tests/test_summarize.py` with 13 tests covering all modes, bullet counts, whitespace stripp...
  - Added structured error handling for authentication, rate limit, timeout, and general API errors
  - Implemented Claude API integration using `anthropic.Anthropic()` client with `claude-haiku-4-5-20251...
  - Added a strict system prompt enforcing bullet-only output with `•` prefix, ≤15 words per bullet,...
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Implemented Claude API integration using `anthropic.Anthropic()` with configurable model via `CONSUM...
  - Added error handling for authentication, rate limit, timeout, and generic API errors
  - Implemented system prompt enforcing bullet-point-only output with `•` character prefix
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Implemented bullet-point prompts with configurable counts (`SHORT_BULLETS=3`, `DEFAULT_BULLETS=5`, `...
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Created `tests/test_summarize.py` with 13 unit tests covering all modes, error paths, model selectio...
  - Implemented Anthropic API call using `claude-haiku-4-5-20251001` model (configurable via `CONSUME_MO...
  - Added structured system prompt enforcing bullet-point output format with `•` character
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and general `AP...
  - Implemented three modes: `short` (3 bullets), `default` (5 bullets), and `long` (8–10 bullets)
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Implemented three summary modes: `short` (3 bullets), `default` (5 bullets), and `long` (8–10 bull...
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Implemented Claude Haiku (`claude-haiku-4-5-20251001`) as the default model via `DEFAULT_MODEL`
  - Added support for three modes: `"short"` (3 bullets), `"default"` (5 bullets), `"long"` (8-10 bullet...
  - Implemented three modes: `short` (3 bullets), `default` (5 bullets), and `long` (8–10 bullets)
  - Added a strict system prompt enforcing bullet-only output with `•` character format
  - Implemented three modes: `short` (3 bullets), `default` (5 bullets), and `long` (8–10 bullets)
  - Added a strict `SYSTEM_PROMPT` enforcing bullet-only output with `•` markers, ≤15 words per bull...
  - Implemented three summary modes: `short` (3 bullets), `default` (5 bullets), and `long` (8-10 bullet...
  - Added a strict `SYSTEM_PROMPT` ensuring output contains only `•`-prefixed bullet points with factu...
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Implemented three summary modes: `short` (3 bullets), `default` (5 bullets), `long` (8–10 bullets)
  - Added a strict system prompt enforcing `•`-prefixed bullet-point output with no editorializing
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Implemented three summary modes: `short` (3 bullets), `default` (5 bullets), and `long` (8–10 bull...
  - Added a strict system prompt enforcing `•`-prefixed bullet-only output with no editorializing
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - [x] Added `CONSUME_MODEL` environment variable override for the model used
  - Added `CONSUME_MODEL` environment variable support in `consume/summarizer.py` via `os.environ.get("C...
  - Added tests in `tests/test_summarize.py` verifying the default model is used when env var is absent ...
  - Implemented three modes: `short` (3 bullets), `default` (5 bullets), and `long` (8–10 bullets)
  - Added a strict `SYSTEM_PROMPT` enforcing bullet-only output with `•` markers, ≤15 words per bull...
  - Added `CONSUME_MODEL` env var override so the model can be swapped without code changes
  - Implemented three output modes: `short` (3 bullets), `long` (8–10 bullets), and `default` (5 bulle...
  - Added a strict `SYSTEM_PROMPT` enforcing bullet-only output with `•` markers, fact-based content, ...
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
  - Implemented three summary modes: `short` (3 bullets), `default` (5 bullets), and `long` (8–10 bull...
  - Added a strict system prompt enforcing bullet-only output with `•` prefix, ≤15 words per bullet,...
  - Implemented three summarization modes: `short` (3 bullets), `default` (5 bullets), and `long` (8–1...
  - Added a strict `SYSTEM_PROMPT` enforcing bullet-only output starting with `•`, ≤15 words each
  - Implemented three summary modes: `short` (3 bullets), `default` (5 bullets), `long` (8–10 bullets)
  - Added strict system prompt enforcing bullet-only output format with `•` character prefix
  - [x] Updated `cli.py` `main()` to run the full fetch → extract → summarize → print pipeline with er...
  - [x] Updated `main()` in `consume/cli.py` to run the full pipeline: `fetch_html` → `extract_text` → `...
  - [x] Added error handling for `ValueError`, `TimeoutError`, `ConnectionError` from `fetch_html`, `ValueEr...
  - [x] Added `TestMainFetchErrors` class with 4 tests covering `ValueError`, `TimeoutError`, `ConnectionErr...
  - [x] Added `TestMainFetchErrors` class to `tests/test_cli.py` with 4 tests covering `ValueError` (invalid...
  - [x] Added `TestMainFetchErrors` class to `tests/test_cli.py` with 4 tests covering `ValueError` (invalid...
  - [x] Added `TestMainFetchErrors` class to `tests/test_cli.py` with 4 tests covering `ValueError` (invalid...
  - [x] Added `TestMainFetchErrors` class to `tests/test_cli.py` with 4 tests covering `ValueError` (invalid...
  - [x] Added `TestMainFetchErrors` class to `tests/test_cli.py` with 4 tests: `ValueError` (invalid URL), `...
  - Added `TestMainFetchErrors` class to `tests/test_cli.py` with 4 tests covering fetch error scenarios...
  - Added `TestMainSummarizeErrors` class with 2 tests covering `RuntimeError` and unexpected exceptions...
  - Added `format_bullets()` helper that normalizes bullet markers (`-`, `*`, `•`) and wraps lines to ...
- [x] Implement clean bullet-point terminal output formatter in `cli.py`
  - Implemented `format_bullets(summary: str) -> str` in `cli.py` that parses `•`-prefixed lines, norm...
  - [x] Created `tests/test_cli.py` with 9 unit tests covering all formatting behaviours (blank removal, mar...
  - [x] Created `tests/test_cli.py` with 9 `format_bullets` unit tests covering: basic bullet preservation, ...
  - Created `tests/test_cli.py` with 9 `format_bullets` unit tests covering: basic bullet preservation, ...
  - Added 3 additional `TestMainEmptyContent` tests for `main()` error handling (empty content, too-shor...

## Phase 3: Article Extraction
- [x] Implement `extractor.py` with `fetch_html(url)` using `requests` (timeout, user-agent header)
  - [x] Implemented `fetch_html(url)` in `extractor.py` using `requests.get` with a 10-second timeout and a ...
  - Implemented `fetch_html(url)` in `extractor.py` was already present — confirmed it uses `requests....
  - [x] Created `tests/test_fetch_html.py` with 7 unit tests covering success, User-Agent header, invalid UR...
  - [x] Created `tests/test_fetch_html.py` with 7 unit tests covering: success (returns HTML), User-Agent he...
  - Created `tests/test_fetch_html.py` with 7 unit tests covering: success (returns HTML), User-Agent he...
  - [x] Updated `pyproject.toml` to add `[project.optional-dependencies]` with `pytest` and `requests-mock` ...
  - Updated `pyproject.toml` already contains `[project.optional-dependencies]` with `pytest` and `reque...
- [x] Implement `extract_content(html)` using `readability-lxml` to strip boilerplate and return main text
  - Renamed `extract_text` to `extract_content` as the primary implementation, using `readability-lxml`'...
- [x] Implement fallback extraction for minimal pages (e.g. strip all tags, return raw visible text)
  - Implemented fallback in `extract_text()`: if `readability` returns empty content, strip all tags dir...
  - Added `test_fallback_to_raw_text_when_readability_returns_empty` to cover the fallback path
- [x] Add content length check — raise extraction failure if result is below a minimum threshold
  - Added `MIN_CONTENT_LENGTH = 50` constant to `extractor.py`
  - Added length check in `extract_text()` that raises `ValueError` with a descriptive message (includin...
  - Added three new tests: `test_too_short_content_raises`, `test_content_at_minimum_length_does_not_rai...
  - Updated `minimal_page.html` fixture to contain enough text to pass the new threshold
  - Updated `test_fallback_to_raw_text_when_readability_returns_empty` to use content long enough to pas...

## Phase 4: Summarization
- [x] Implement `summarizer.py` with `summarize(text, mode)` that calls the configured LLM API
  - Implemented `summarize(text, mode)` in `summarizer.py` calling the Anthropic API with configurable m...
  - Added `"short"` mode (3 bullets) and `"long"` mode (9 bullets) via `SHORT_BULLETS`/`LONG_BULLETS` co...
  - Added error handling for `AuthenticationError`, `RateLimitError`, `APITimeoutError`, and generic `AP...
- [x] Write the system prompt enforcing factual, bullet-only output (default: 5 bullets, ≤15 words each)
  - Updated `SHORT_BULLETS` from 3 to 5 to match the required default bullet count
- [x] Implement mode-based prompt variation: `short` → 3 bullets, `long` → 8–10 bullets, `default` → 5 bullets
  - Updated `SHORT_BULLETS = 3`, added `DEFAULT_BULLETS = 5`, replaced `LONG_BULLETS` with `LONG_BULLETS...
  - [x] Changed `summarize()` default parameter from `mode="short"` to `mode="default"`
  - Implemented three-branch prompt logic: `short` → "exactly 3 bullets", `long` → "between 8 and 10...
  - Updated system prompt rule #5 wording to accommodate ranges
  - Updated test imports to use new constant names
- [x] Make LLM provider/model configurable via environment variable
  - Updated `summarizer.py` to read the `CONSUME_MODEL` environment variable (falling back to `DEFAULT_M...
  - Added `DEFAULT_MODEL` constant and `import os` to `summarizer.py`
  - Added two new unit tests: `test_uses_default_model_when_env_not_set` and `test_uses_model_from_env_v...

## Phase 5: Error Handling
- [x] Handle invalid URL error with a clear user-facing message and non-zero exit code
  - [x] Added `_validate_url()` to `extractor.py` that checks for `http`/`https` scheme and non-empty host, ...
  - [x] Added `_validate_url(url: str) -> None` to `consume/extractor.py` that checks for `http`/`https` sch...
  - Added `_validate_url(url: str) -> None` to `consume/extractor.py` — already present, checking `htt...
  - Created `tests/test_url_validation.py` with 11 test cases covering valid HTTP/HTTPS URLs, missing sc...
  - Updated `cli.py` to catch `ValueError` separately and print a clean `Error: ...` message (without th...
- [x] Handle network failure (connection error, timeout) with a clear user-facing message
  - Added `requests.exceptions.Timeout` handling in `fetch_html` that raises a `TimeoutError` with a cle...
  - Added `requests.exceptions.ConnectionError` handling in `fetch_html` that raises a `ConnectionError`...
  - Updated `cli.py` to catch `TimeoutError` and `ConnectionError` separately, printing their messages t...
- [x] Handle extraction failure (no content parsed) with a clear user-facing message
  - Updated `extract_text` in `extractor.py` to raise `ValueError` with a clear message when no content ...
  - Updated `cli.py` to wrap `extract_text` in a `try/except ValueError` block, printing the error to st...
- [x] Handle empty content after extraction with a clear user-facing message
  - Updated `test_extract_content.py` to match the new error message wording
  - Added `TestMainEmptyContent` class to `test_cli.py` with three tests covering: empty content prints ...
- [x] Handle LLM API failure (auth error, rate limit, timeout) with a clear user-facing message
  - Added try/except in `summarize()` catching `anthropic.AuthenticationError`, `anthropic.RateLimitErro...
  - Updated `cli.py` to catch `RuntimeError` from the summarizer and print the message to stderr with ex...

## Phase 6: Utilities
- [x] Implement `utils.py` with shared helpers: text truncation (cap input tokens to LLM), whitespace normalization
  - [x] Created `consume/utils.py` with two shared helpers:
  - Created `consume/utils.py` with `normalize_whitespace(text)` (collapses whitespace via regex) and `t...
  - Updated `consume/extractor.py` to import and use `normalize_whitespace` in `_strip_tags`, removing d...
  - Updated `consume/summarizer.py` to import `truncate_text` and apply it at the start of `summarize()`...

## Phase 7: Testing
- [x] Write unit tests for URL validation logic
  - [x] Created `tests/__init__.py` to make the test directory a package
  - [x] Created `tests/__init__.py` as an empty file to mark the `tests` directory as a Python package
  - [x] Created `tests/__init__.py` as an empty file to mark the `tests` directory as a Python package
  - [x] Created `tests/__init__.py` as an empty file to mark the `tests` directory as a Python package (alre...
  - Created `tests/test_url_validation.py` with 11 unit tests covering `_validate_url`:
- [x] Write unit tests for `extract_content()` using fixture HTML files (normal page, minimal page)
  - Created `tests/fixtures/normal_page.html` — a realistic article page with HTML entities, navigatio...
  - Created `tests/fixtures/minimal_page.html` — a bare-bones page with a single paragraph
  - Created `tests/test_extract_content.py` with 10 tests covering: text extraction from both fixtures, ...
- [x] Write unit tests for `summarize()` by mocking the LLM API response
  - Created `tests/test_summarize.py` with 11 unit tests covering `summarize()` using `unittest.mock.pat...
  - Added tests for short and long mode returning the API response text
  - Added tests verifying the correct bullet count is passed in the prompt for each mode
  - Added a test confirming the default mode is short
  - Added tests verifying the input text is forwarded to the API
- [x] Write an integration smoke test: run `consume <real_url>` and assert bullet output is returned
  - [x] Created `tests/test_integration_smoke.py` with a `@pytest.mark.integration` smoke test that invokes ...
  - [x] Created `tests/test_integration_smoke.py` with a `@pytest.mark.integration` smoke test
  - [x] Created `tests/test_integration_smoke.py` with a `@pytest.mark.integration` smoke test that invokes ...
  - [x] Created `tests/test_integration_smoke.py` with a `@pytest.mark.integration` smoke test that invokes ...
  - [x] Created `tests/test_integration_smoke.py` with a `@pytest.mark.integration` smoke test that invokes ...
  - [x] Created `tests/test_integration_smoke.py` with `@pytest.mark.integration` smoke test that runs `cons...
  - [x] Created `tests/test_integration_smoke.py` with a `@pytest.mark.integration` smoke test that runs `co...
  - Created `tests/test_integration_smoke.py` with a `@pytest.mark.integration` smoke test that runs `co...
  - Added graceful skip logic for network/API failures (connection errors, timeouts, auth failures, rate...
  - Added assertion that output contains at least one bullet line starting with `•`
  - Added graceful skip logic for network/API failures (`Could not connect`, `timed out`, `authenticatio...
  - Added assertions for non-zero exit code, non-empty output, and at least one bullet line starting wit...
  - Added skip logic for network/API failures so CI without credentials degrades gracefully
  - [x] Added `[project.scripts]` entry to `pyproject.toml` so `consume` is a proper CLI entry point
  - Added `[tool.pytest.ini_options]` with the `integration` marker registration to avoid unknown-mark w...

## Phase 8: Documentation
- [x] Write `README.md` covering: installation, required env vars, usage examples, supported modes
  - [x] Created `README.md` covering installation steps, the required `ANTHROPIC_API_KEY` env var, CLI usage...
  - [x] Updated `README.md` to add `.env` file setup instructions (copy from `.env.example`) alongside the d...
  - Updated the Environment Variables table to include the optional `CONSUME_MODEL` variable from `.env....
  - Updated the copy instruction wording to "fill in your values" to reflect that there are now multiple...
  - Added a "Running Tests" section covering how to run unit tests only (`-m "not integration"`) vs. the...
