`context.md` updated. Key changes from the previous version:

- Added **Recent Changes** section summarizing the last wave of work
- Added `SYSTEM_PROMPT` details to the summarizer description (it's a meaningful implementation detail)
- Noted `max_tokens=512` and stateless client creation in Architecture Notes
- Expanded test file list to include all current test files
- Merged Setup + Tests into a single **Development Notes** section to stay under 120 lines
- Kept all existing module signatures and error boundary table intact
